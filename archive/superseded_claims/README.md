# archive/superseded_claims/

Claims this repository used to make and no longer makes. Kept because a reader
who finds the old wording elsewhere — in a draft, a slide, an email — needs to
be able to look up what replaced it.

| Superseded claim | Replaced by | When |
|---|---|---|
| "No counterexample to matched-readout positivity was found in 150 random draws" (F5B) | The F5C identity `Re χ_matched = x†Γx ≥ 0` — a proof, not a scan (`CLAIMS.md` C3) | 2026-08 |
| "`any_gain_in_matched = true`" (F5B artifact, pre-bugfix run) | Regenerated artifact: `min_Re_chi_matched = +0.012866` at `δ = −6.0`, `any_gain_in_matched = false` | 2026-08-10 |
| "A measurable design rule across engineered-dissipation platforms" | The narrow 3-mode conditional PASS (`CLAIMS.md` C7); the general form is refuted (`NON_CLAIMS.md` N1) | 2026-08-02, Gate E |
| Binary go/no-go criterion for NV | The three-tier trichotomy in `ν` (`CLAIMS.md` C1), which contains the binary criterion as its `ν = ∞` vs. finite split | — |
| Dark-state presence as the diagnosis of EIT | The response-theoretic definition (`CLAIMS.md` C4) and the dark-state-free witness (C5) | — |

A superseded claim may not be revived by rewording. It can only return with
new evidence that closes the specific gap that killed it.
