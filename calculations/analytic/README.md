# calculations/analytic/

Symbolic and analytic derivations.

| Path | Contents |
|---|---|
| `master-equation/EIT in 6 Levels.ipynb` | The 6-level EIT master equation, set up and reduced. |
| `master-equation/Mathmatical formula.ipynb` | Symbolic derivations supporting the response formulas. |

These notebooks are derivation records, not part of the gate pipeline: nothing
in `../../results/` is produced by them. Run them with any Jupyter kernel that
has `sympy` available.
